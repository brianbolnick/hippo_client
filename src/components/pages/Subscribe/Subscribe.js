import React from "react";
import { Elements } from "react-stripe-elements";
import Form from "./Form";

const View = () => {
  return (
    <Elements>
      <Form />
    </Elements>
  );
};

export default View;
